# ============================================================
# API Backend
# Android emulator  → gunakan 10.0.2.2 (alias localhost emulator)
# HP fisik          → gunakan IP lokal mesin developer (cek via ifconfig)
# Production        → https://api.solekita.id/v1
# ============================================================
API_URL=http://10.0.2.2:8080/v1
